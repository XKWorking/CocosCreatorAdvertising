export enum LangType {
    ZH_CN,
    ZH_TW,
    EN,
    KO,
    JA,
    DE,
    FR,
    ES,
    PT,
    IT,
    BE,
    UK,
    ID,
    AR
}


export enum AnimeTypes {
    FADEIN,
    LEFTIN,
    TOPIN,
    SCALEIN
}

export enum DirectionType {
    W,
    E,
    S,
    N,
    SW,
    SE,
    NW,
    NE,
    ERROR
}

export enum ActionNodeType {
    BreathScale,
    rotate,
}

export enum ActionSlideType {
    /**滑动 */
    Slide,
    /**滑动并晃动 */
    SlideSaggle,
}

export enum DuraLevel {
    Fast,
    Medium,
    Slow
}

export enum ActionShowType {
    Back,
    FadeIn,
    ScaleIn,
}
export enum ActionHiddenType {
    BackOut,
    FadeOut,
    ScaleOut,
}

export enum PercentLevel {
    Short,
    Medium,
    Long,
}

